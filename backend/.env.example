# Required: set in GitHub Secrets / cloud runtime; do not commit .env with real values
DATABASE_URL=
JWT_SECRET=

# Optional
PORT=

# Optional: for db:seed demo user only (omit in production)
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
